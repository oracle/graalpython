[build-system]
# We pin the setuptools version to avoid any surprises from upstream
# We stick to the version that is bundled with GraalPy in ensurepip
requires = ["setuptools==80.3.1", "wheel==0.45.1"]
build-backend = "setuptools.build_meta"
